/**
 * Public API for the AI layer.
 * The API route imports only from here — not from individual AI submodules.
 */
export { analyzePage } from './analyze-page';
export type { AnalyzePageResult } from './analyze-page';
